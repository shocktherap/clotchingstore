<h3><i class="fa fa-angle-right"></i> Product Merk</h3>
<div class="row mt">
  <div class="col-lg-12">
    <div class="content-panel">
      <section id="unseen">
        <div class="unseen-margin">
          <%= page_entries_info @merks %>
          <%= link_to 'New', new_backend_vendor_path, class: 'btn btn-theme03 pull-right button-margin' %>
          <table class="table table-bordered table-striped table-condensed">
            <thead>
              <tr>
                <th>Name</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
              <% @merks.each_with_index do |merk| %>
                <tr>
                  <td><%= link_to merk.name, backend_vendor_path(merk) %></td>
                  <td>
                    <%= link_to edit_backend_vendor_path(merk), class: 'btn btn-primary btn-xs' do %>
                      <i class="fa fa-pencil"></i>
                    <% end %>
                    <%= link_to backend_vendor_path(merk), method: :delete,class: 'btn btn-danger btn-xs',data: { confirm: 'Are you sure to delete?' } do %>
                      <i class="fa fa-trash-o "></i>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
          <%= paginate @merks %>
        </div>
      </section>
    </div>
  </div>
</div>
